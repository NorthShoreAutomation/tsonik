{
  "name": "tsonik-examples",
  "version": "1.0.0",
  "private": true,
  "description": "Working TypeScript examples for Tsonik documentation",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/**/*.ts",
    "check": "npm run lint && npm run build"
  },
  "dependencies": {
    "tsonik": "file:.."
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.12.0",
    "@typescript-eslint/parser": "^6.12.0",
    "eslint": "^8.54.0",
    "typescript": "^5.3.2"
  }
}